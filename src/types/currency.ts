export enum ECurrency {
  // the USD fiat currency
  USD = 'USD',
  // the USDC token
  USDC = 'USDC',
  // the USDT token
  USDT = 'USDT',
  // the ETH token
  ETH = 'ETH',
  // the BTC token
  BTC = 'BTC',
  // the SOL token
  SOL = 'SOL',
  // the ARB token
  ARB = 'ARB',
  // the BNB token
  BNB = 'BNB',
  // the ZK token
  ZK = 'ZK',
  // the POL token
  POL = 'POL',
  // the OP token
  OP = 'OP',
  // the ATOM token
  ATOM = 'ATOM',
  // the 1000PEPE token
  KPEPE = 'KPEPE',
  // the TON token
  TON = 'TON',
  // the XRP token
  XRP = 'XRP',
  // the XLM token
  XLM = 'XLM',
  // the WLD token
  WLD = 'WLD',
  // the WIF token
  WIF = 'WIF',
  // the VIRTUAL token
  VIRTUAL = 'VIRTUAL',
  // the TRUMP token
  TRUMP = 'TRUMP',
  // the SUI token
  SUI = 'SUI',
  // the 1000SHIB token
  KSHIB = 'KSHIB',
  // the POPCAT token
  POPCAT = 'POPCAT',
  // the PENGU token
  PENGU = 'PENGU',
  // the LINK token
  LINK = 'LINK',
  // the 1000BONK token
  KBONK = 'KBONK',
  // the JUP token
  JUP = 'JUP',
  // the FARTCOIN token
  FARTCOIN = 'FARTCOIN',
  // the ENA token
  ENA = 'ENA',
  // the DOGE token
  DOGE = 'DOGE',
  // the AIXBT token
  AIXBT = 'AIXBT',
  // the AI16Z token
  AI16Z = 'AI16Z',
  // the ADA token
  ADA = 'ADA',
  // the AAVE token
  AAVE = 'AAVE',
  // the BERA token
  BERA = 'BERA',
  // the VINE token
  VINE = 'VINE',
  // the PENDLE token
  PENDLE = 'PENDLE',
  // the UXLINK token
  UXLINK = 'UXLINK',
  // the KAITO token
  KAITO = 'KAITO',
  // the IP token
  IP = 'IP',
}
